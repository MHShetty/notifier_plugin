import 'package:flutter/material.dart';

class SWNotifierExample extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("SWNotifier Example"),),
    );  }
}
